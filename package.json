{
  "name": "history-sleep",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173 -d .",
    "check": "node --check src/app.js && node --check src/stories.js && node --check scripts/generate-audio.mjs",
    "generate:audio": "node scripts/generate-audio.mjs",
    "list": "node scripts/generate-audio.mjs --list"
  }
}
